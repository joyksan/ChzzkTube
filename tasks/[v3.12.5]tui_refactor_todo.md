# [v3.12.5] TUI 인터랙티브 개편 작업 목록 (SPEC-2026-UI-002 v2.0)

## Task 1: 폰트 렌더링 최적화 및 DPI 보정 (완료)
- [x] `main_window.py`의 `main()`에서 `QApplication.setHighDpiScaleFactorRoundingPolicy(PassThrough)` 적용
- [x] `ui/log_console.py`에서 콘솔 전용 `QFont` 설정
  - [x] `Cascadia Mono` 폰트 지정
  - [x] `PreferNoHinting` 및 `NoSubpixelAntialias | PreferAntialias` 적용
  - [x] `Weight.Medium` 적용
  - [x] `AbsoluteSpacing` 0.0 적용
  - [x] `self.te.viewport().setAttribute(WA_OpaquePaintEvent, True)` 적용

## Task 2: 상단 핫키 바 투톤 타이포그래피 및 구분자 개선 (완료)
- [x] 대괄호 및 파이프 제거, 미들 닷(`·`) 구분자 적용 (`F1 Change · F2 Open · F12 Log · F3 Settings`)
- [x] 단축키 바 투톤 스타일 적용: 키 심볼(`#E5E5E5`) + 액션 텍스트(`#8A8A8A`), 호버 시 민트(`#00D8B4`) 하이라이트
- [x] 하단 액션 버튼 라벨 갱신 (`F4 Load .txt · ESC Clear · ↵ Start`)
- [x] 버튼 위젯 식별을 위한 `setObjectName` 전면 부여 (`btn_change`, `btn_open`, `btn_esc`, `btn_enter` 등)

## Task 3: Sleek Dash & Head 프로그레스 바 글리프 사양 적용 (완료)
- [x] `_log_bar(bar_frac, width=16)` 함수 사양 갱신
  - [x] Filled Track: `━` (U+2501)
  - [x] Indicator Head: `╸` (U+2578)
  - [x] Empty Track: `┈` (U+2508)
  - [x] 16칸 고정폭 유지

## Task 4: 일반 실행 시 10줄 헬스체크 완벽 은폐 & 1줄 요약 뱃지 버그 해결 (완료)
- [x] `update_worker.py`의 `_do_check`에서 `pot` 상태(`SKIP`)를 정상으로 판별하도록 조건 수정 (`status in ("OK", "SKIP")`)
- [x] 모든 정상 헬스체크 및 절대 경로를 F12 상세 로그(`to_tui=False`)로 격리
- [x] 메인 콘솔에는 `✔ Environment ready  (ytdlp ... · ffmpeg ... · node ... · pot active)` 1줄만 표출되도록 완전 정돈

## Task 5: State 1 & State 3 UX 인터랙션 완성 (완료)
- [x] State 1 (수급 중): 인풋 필드 플레이스홀더를 `> Initializing portable runtime environment... (ESC Cancel)`로 설정 및 딤(Dim) 처리
- [x] State 3 (다운로드 진행 중): 상단 인풋 상태 라벨을 `[ESC] Abort · [F12] Detailed Log`로 전환 연동
- [x] 상태 태그 컬러 팔레트(`FETCH` #00D8B4, `QUEUE` #707070, `✔`/`OK` #00D8B4, `ERR` #FF5555) 스타일링

## Task 6: MCP Tool을 활용한 실시간 E2E 시각 검증 (완료)
- [x] `qt-pilot`으로 앱 기동 후 `after_ui.png` 및 `final_ui.png` 캡처하여 1줄 성공 뱃지 및 깨끗한 여백 확인
- [x] `type_text` 및 `click_widget` 상호작용 검증 (`url_input` 타이핑 -> `btn_esc` 클릭 후 클리어)
- [x] 전체 회귀 테스트(`pytest` 395개 항목) 100% 통과 확인 (392 passed, 3 skipped, 0 failed)
